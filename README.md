### Name: Yoohyuk Chang
### Under `q2` directory, it contains code for `q2`, and under `q3` directory, it contains necessary code for `q3` (Unity code).

### For Q2.py, simply run the Python script and you'll get the plot and result.

### To run Q3 (unity), please download the entire folder `q3` and import this folder to Unity. And then, run by clicking the play button.